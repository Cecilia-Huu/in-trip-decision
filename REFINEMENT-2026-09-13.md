# 移动端 refinement 验收记录（2026-09-13）

这是现有 React / Vite 项目的增量修改。仓库、GitHub Pages 路径、首页、按需补问、两个 Tab 和识景 Beta 保留。没有新增模型、Places、路线或其他外部 API。

## 文件修改

| 文件 | 本轮改动 |
| --- | --- |
| app/decision-engine.ts | 设备本地时钟、具体活动类别、两段建议时间窗口、固定安排临近/已过/远期边界；复合微调意愿与约束；类别对应地图查询。保留原地图链接实现。 |
| app/context-parser.ts | 支持“走得很累”“睡一觉”等表达，同时保留“还想逛”和“别走远”；酒店休息无需追问安排；只有明确雨情/室内偏好才作为室内约束。 |
| app/progressive-context.tsx | 提交时读取真实设备时钟和约束；接入共享小鹅过渡。保留点击定位、成功坐标、失败手动回退和晚到请求取消逻辑。 |
| app/page.tsx | Adjust chip 只填草稿，可继续编辑；点击重新调整才更新策略；原地小鹅过渡，保留地点、坐标、Anchor，刷新计算时刻。无法匹配的调整会明确提示，不随机换方案。 |
| app/goose-state.tsx | 共享 Thinking / Location 小鹅状态和可取消的 650ms 视觉过渡，不代表 LLM 或网络耗时。 |
| public/assets/goose-location.svg | 基于本项目原创小鹅新增问号状态，没有复制用户参考图片。 |
| public/assets/goose-thinking.svg | 沿用上一轮为此项目绘制的原创 SVG，本轮已真实展示于首次生成和重新调整。 |
| app/globals.css | 小鹅 96px、Adjust 输入和 CTA、重新调整遮层及时间段结束提示；未重做桌面端。沿用 reduced-motion。 |
| index.html | 预加载 Thinking 小鹅，降低首次展示时图片尚未到达的概率。 |
| tests/decision.test.mjs | 扩充到 24 项规则、时间、地图、定位适配器和 Lens 回归。 |
| .github/workflows/deploy-pages.yml | 原 GitHub Pages 发布流程加入自动化测试。 |

## 时间和类别如何工作

首次提交和每次确认微调时通过 new Date() 读取当前设备时间，getHours()/getMinutes() 生成 currentLocalTime，ISO 时间继续保存在 currentTime。自然语言中的时间仍保留为输入信息，不冒充 GPS 所在城市的时区；界面明确使用设备本地时间。

无固定安排时，本轮安排两个约 40 分钟窗口，并用一行文字提示第二段结束后再决定，不增加第三个活动。例：10:20–11:00、11:00–11:40。睡觉场景第二段有条件执行，不要求按点醒来。

Anchor 在未来 1–3 小时内时，首段最多 40 分钟，并预留一段移动窗口。例如 17:20 / 19:00 晚餐：17:20–18:00 休息、18:00 后开始向晚餐地点移动。预留窗口不是路线时间，必须由地图确认；没有保证能按时到达。距离安排不超过一小时则不插入新活动；设备时间已过或 AM/PM 未知则先核对，不擅自改到明天。超过三小时的 Anchor 仅保留为后续安排，不把整个空档排满。跨午夜显示“次日”。

疲惫/休息：咖啡馆、面包店；继续探索：公园、当前街区、小店区域；换个感觉或明确室内需求：商场、室内公共空间。复合输入“我还想逛，但不要走太远”保留探索意图，并限制在当前区域。没有状态时采取坐一会儿再附近轻逛的保守方案。不使用“轻量落脚点”，不臆造具体店名或营业事实。

## 地图与定位

地图 CTA 指明本步所搜类型，查询词分别为 cafes / parks / shopping malls，加手动当前位置；有真实坐标则使用 Apple 的搜索中心或 Google 的类别+坐标搜索表达。导航步骤只使用已提供的 Anchor 地点，未提供地点不编导航终点。查询内容 URL 编码，新标签打开，继续记忆用户上次地图选择；坐标模式不提供未经坐标系转换验证的高德入口，手动地点仍保留高德搜索。

定位仍只在点击“使用当前位置”后调用 getCurrentPosition。成功只保存 latitude/longitude，不把坐标伪装成景点名；失败、超时、不支持均进入小鹅错误状态和手动入口。切到手动后迟到的定位回调不会覆盖输入。无反向地理编码服务。

## CASE A–E：真实结果及限制

| CASE | 结果 |
| --- | --- |
| A 完整地点+疲惫+晚餐 | 浏览器通过：无重复补问，截图捕获首次小鹅，读取设备时间，咖啡馆/面包店首步，保留19:00。17:20→18:00→19:00的特定时间用确定性单元测试通过；实际浏览器当时为上午，因此只排眼前约80分钟，不让用户上午就去晚餐。 |
| B 突然多出时间、无安排 | 浏览器手动地点分支通过：两段真实设备时间起算窗口，无假Anchor。坐标成功+地图中心通过合成坐标自动化测试；当前环境未取得一次真实GPS成功，不能声称这一部分已真机通过。 |
| C 酒店只想睡一觉 | 浏览器通过：跳过安排和状态补问，显示小鹅后进入休息策略，第二段明确可不执行。 |
| D 定位失败 | 浏览器真实进入失败状态，显示问号小鹅。此后语义点击和原生点击都无页面变化，且未发现网页JavaScript对话框；环境/权限层原因未确定。故失败→手动→结果完整链路尚未认证。重新加载后，普通手动入口可正常输入并完成主流程。失败适配器的拒绝/超时/不支持分支自动化通过。 |
| E 草稿式微调 | 浏览器通过：chip 填入“我还想逛”，结果不变；编辑成“我还想逛，但不要走太远”并确认，截图捕获短小鹅；原地更新成公园/小店探索，Why同步改变，Anchor保留。 |

## 其他回归

24 项 npm test 通过；构建、lint、差异检查通过。375/390/430px 的首页、结果、识景页面无横向溢出，首页CTA高于底部导航，主输入/调整输入为16px。属于浏览器视口测试，不等于所有实体iPhone/Safari认证。

识景 Beta：米兰/塞维利亚/圣家堂匹配通过；米兰30秒/故事/详细内容真实切换；英文名称切换和未知地点提示通过。app/mock-data.ts 未修改。

真实地图测试：公园CTA打开Google Maps新标签，URL和页面标题为米兰大教堂附近公园搜索；不是晚餐区域，也没有自画地图。各地图原生App的系统级唤起仍由设备决定。

## 明确的非能力

自然语言解析、意图和约束提取、类别选择、建议时间窗口仍是有限规则。没有LLM backend、前端API Key、真实POI/路线API、天气获取、评分、营业时间、排队或座位数据。650ms只是用户可见的UI过渡，不伪装成“深度AI思考”。时间窗口是建议分配，不是交通ETA；推断的空间距离/顺路关系从未当作事实输出。
